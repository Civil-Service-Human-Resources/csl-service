### REF: https://docs.spring.io/spring-boot/docs/current/reference/html/application-properties.html

## Actuator properties
# Change base-path
management.endpoints.web.base-path = /manage
# By default, only two endpoints are exposed: /health and /info.
# Expose all endpoints
management.endpoints.web.exposure.include = *
# Health
management.endpoint.health.show-details = always
# Azure Health Indicators
management.health.azure-appconfiguration.enabled = true
# Info
management.info.java.enabled = true
management.info.env.enabled = true
info.app.name = csl-service
info.app.description = csl-service description here
info.app.mvnw.version = Apache Maven 3.8.6
info.app.type = Spring Boot

## Spring Boot Developer tools
spring.devtools.livereload.enabled = false

## Spring Security properties - Required to access the actuator endpoints
spring.security.user.name = ${SECURITY_USERNAME:csl-service-security-user}
spring.security.user.password = ${SECURITY_PASSWORD:csl-service-security-password}

## Logging properties
# Root
logging.level.root = ${ROOT_LOGGING_LEVEL:INFO}
# Spring Framework
logging.level.org.springframework.security = ${SPRING_SECURITY_LOGGING_LEVEL:DEBUG}
logging.level.org.springframework.http = ${SPRING_HTTP_LOGGING_LEVEL:DEBUG}
logging.level.org.apache.http = ${APACHE_HTTP_LOGGING_LEVEL:DEBUG}
# CSL Application
logging.level.uk.gov.cabinetoffice = ${CSL_SERVICE_LOGGING_LEVEL:DEBUG}

## Server properties
# Server port
server.port = ${CSL_SERVICE_PORT:9003}
# Display name of the application
server.servlet.application-display-name = ${info.app.name}
# Context path of the application
server.servlet.context-path = ${CSL_SERVICE_CONTEXT-PATH:/csl-service}
#Session timeout (seconds)
server.servlet.session.timeout = ${SESSION_TIMEOUT:3600}

## identity oauth properties
#oauth.serviceUrl = ${OAUTH_SERVICE_URL:https://identity.integration.learn.civilservice.gov.uk}
oauth.serviceUrl = ${OAUTH_SERVICE_URL:http://localhost:8080}
oauth.tokenUrl = ${oauth.serviceUrl}/oauth/token
oauth.clientId = ${CLIENT_ID:9fbd4ae2-2db3-44c8-9544-88e80255b56e}
oauth.clientSecret = ${CLIENT_SECRET:test}
oauth.jwtKey = ${JWT_KEY:blah}
oauth.maxTotalConnections = ${MAX_TOTAL_CONNECTIONS:120}
oauth.defaultMaxConnectionsPerRoute = ${DEFAULT_MAX_CONNECTIONS_PER_ROUTE:60}
