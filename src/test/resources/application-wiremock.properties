wiremock.reset-mappings-after-each-test=true
wiremock.port=9000
wiremock.stubs.reportService.baseUrl=report-service
reportService.serviceUrl=http://localhost:${wiremock.port}/${wiremock.stubs.reportService.baseUrl}
wiremock.stubs.learnerRecord.baseUrl=learner_record_api
learnerRecord.serviceUrl=http://localhost:${wiremock.port}/${wiremock.stubs.learnerRecord.baseUrl}
wiremock.stubs.notificationService.baseUrl=notification_service
notificationService.serviceUrl=http://localhost:${wiremock.port}/${wiremock.stubs.notificationService.baseUrl}
wiremock.stubs.rustici.baseUrl=rustici_engine
rustici.serviceUrl=http://localhost:${wiremock.port}/${wiremock.stubs.rustici.baseUrl}
rustici.engineTenantName=test
wiremock.stubs.learningCatalogue.baseUrl=learning_catalogue
learningCatalogue.serviceUrl=http://localhost:${wiremock.port}/${wiremock.stubs.learningCatalogue.baseUrl}
wiremock.stubs.csrs.baseUrl=csrs
csrs.serviceUrl=http://localhost:${wiremock.port}/${wiremock.stubs.csrs.baseUrl}
logging.level.root=DEBUG
logging.level.uk.gov.cabinetoffice.csl.client.HttpClient=DEBUG
logging.level.uk.gov.cabinetoffice.csl.client=DEBUG
